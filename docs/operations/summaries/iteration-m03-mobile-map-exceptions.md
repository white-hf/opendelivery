# M03 移动端地图与异常操作执行总结

状态：`COMPLETED`（2026-07-27）

## 已完成

- 新增通用 `MobileActionBar`，接入订单准备、到仓清单和派送计划。
- 移动端底部操作栏提供打开订单列表、到仓包裹明细和派送包裹列表入口，并显示当前数量。
- 订单准备侧栏、到仓地图/笼板区和派送地图列表在手机宽度下重新排列，保留地图选择与抽屉详情。
- 桌面端不显示底部栏，原有浮动控件保持不变。

## 验证

- Operations Web `pnpm run typecheck` 通过。
- Operations Web `pnpm run build` 通过。
- 构建仅提示既有大 chunk 警告。
