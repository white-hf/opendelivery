# M04 交付总结：移动端派送地图优先布局

状态：COMPLETED

## 实现

- 为派送工作区增加移动端专用布局类；步骤 3/4 在窄屏隐藏固定左栏，地图使用全宽和近似全屏高度。
- 增加地图顶部司机选择按钮与底部抽屉，支持选择司机、清除筛选及显示容量信息。
- 为包裹浮动表格增加窄屏定位和宽度适配，并保留底部“打开派送包裹列表”入口。
- 桌面布局和接口协议未改变。

## 验证

- `pnpm run typecheck`：通过。
- `pnpm run build`：通过。
