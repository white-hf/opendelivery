# M06 交付总结：英文 UI 与国际化清理

状态：COMPLETED

运营 Web 已将区域、到仓、订单准备、派送规划、司机包裹、扫描监督、异常和配置工作台的用户可见硬编码迁移到翻译词典。`en-CA`、`fr-CA`、`zh-CN` 的键集合已对齐，并补齐司机建议文档入口词条。

同时修正了两个已过期测试契约：司机包裹页面属于调度员稳定导航；前端站点上下文使用 `X-Station-Id`。验证结果：26 个 Vitest 测试通过，`pnpm run typecheck` 和 `pnpm run build` 通过。现有 ESLint 技术债仍单独保留，不作为本次国际化完成声明。
