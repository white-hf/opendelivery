# M08 Delivery Summary: Driver Suggestion Entry

Status: COMPLETED

The signed-in top user action area now contains a “Driver suggestion” menu with separate Chinese and English static HTML links, opened in a new tab. Default paths are:

- `/docs/driver-suggestion/index.zh-CN.html`
- `/docs/driver-suggestion/index.en.html`

The paths can be overridden with `VITE_DRIVER_SUGGESTION_DOC_ZH_URL` and `VITE_DRIVER_SUGGESTION_DOC_EN_URL`. Frontend type checking and the production build pass.
