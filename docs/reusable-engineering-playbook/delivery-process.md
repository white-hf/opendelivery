# 敏捷研发流程

## 迭代闭环

```text
问题/目标 → PRD → 架构/契约 → 迭代计划(REVIEWED)
       → 垂直切片开发 → 测试证据 → 执行总结 → 发布/复盘
```

- 一个迭代只承诺可验证的垂直价值切片。
- 先交付最小运营闭环，再扩展自动化、优化和标杆能力。
- 每次变更重新检查依赖、回滚、兼容和数据迁移。
- 未完成项、基线失败和外部阻塞必须显式记录，不用状态文字掩盖。
- 提交前检查文档、代码、测试、迁移和部署配置是否同步。
