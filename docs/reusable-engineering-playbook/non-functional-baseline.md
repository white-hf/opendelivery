# 非功能技术基线

- 性能目标先量化：吞吐、p95/p99、并发、数据规模、可接受陈旧度和资源上限。
- 查询走索引左前缀、分页和必要的覆盖索引；禁止无边界全表加载。
- 历史数据按保留策略归档或分区，读模型与写模型按访问模式设计。
- 指标至少覆盖请求成功率、延迟、数据库连接池、慢查询、队列积压、重试和业务失败率。
- 结构化日志关联 request id 和业务 id，Trace 跨 API、消息和数据库边界传递。
- 配置按环境分离；密钥不入库，最小权限访问数据库和外部 API。
- 明确备份、恢复点目标（RPO）、恢复时间目标（RTO）和演练频率。
