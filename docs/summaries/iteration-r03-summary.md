# R03 到仓实物执行总结

## 已交付

- Flyway V12 新增 `arrival_trip`、`handling_unit`、`handling_unit_parcel`，包含站点唯一键、状态查询索引和外键。
- Trip 支持预计、到达、卸货、可扫描、关闭；板/笼/袋支持预计、到达、开板、清空。非法跳转被拒绝。
- 到仓详情聚合实物单元的包裹、司机和批次覆盖；人工关联追踪号时拒绝未知或跨站包裹。
- 运营 Web 提供 Trip 列表、新建、状态推进、板/笼创建和详情抽屉，并保留原上游 Manifest 差异页。
- 到仓事实不修改包裹状态或 custody；R05 审批才转移司机责任。

## 验证

Java 34 项、Vitest 19 项、Playwright 3 项通过；生产构建和 lint 通过。真实 MySQL 已成功执行 V11→V12，API 验证 Trip/Unit 状态推进、YYZ 读取 YHZ 资源返回 403，测试记录已清理。

## 未关闭范围

R03-C 仍需把上游 Unit 标识自动映射到包裹，并生成三城市、多板跨司机的真实 fixture；当前可通过运营录入追踪号形成覆盖。完成后进入 R04 本人扫描。
