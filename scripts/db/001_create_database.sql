-- Bootstrap only. Business tables are intentionally deferred until the
-- last-mile domain model and state-transition matrix are reviewed.
CREATE DATABASE IF NOT EXISTS `opendelivery`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_0900_ai_ci;

